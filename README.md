<div align="center">

<img src="frontend/public/logo.png" alt="Aletheia Finance Logo" width="80" />

# Aletheia Finance

### *The First Cryptographically Verifiable AI Trading Agent with On-Chain Fundamental Analysis*

**Trade smarter. Analyze deeper. Prove every decision.**

[![License: MIT](https://img.shields.io/badge/License-MIT-white.svg)](https://opensource.org/licenses/MIT)
[![Built on 0G](https://img.shields.io/badge/Built%20on-0G%20Network-blue)](https://0g.ai)
[![Deployed on Galileo](https://img.shields.io/badge/Testnet-0G%20Galileo-green)](https://chainscan-galileo.0g.ai)
[![Go](https://img.shields.io/badge/Backend-Go%201.24-00ADD8?logo=go)](https://go.dev)
[![Next.js](https://img.shields.io/badge/Frontend-Next.js%2015-black?logo=next.js)](https://nextjs.org)
[![Solidity](https://img.shields.io/badge/Contract-Solidity%200.8.24-363636?logo=solidity)](https://soliditylang.org)

[Live Demo](#) · [Smart Contract](https://chainscan-galileo.0g.ai/address/0x9b6eF2eb94Fc756D2521DDc42Baeb4Ec3a3454C4) · [Documentation](#architecture)

</div>

---

## 🧠 What is Aletheia?

Aletheia Finance is a **non-custodial AI trading platform** built on the [0G Network](https://0g.ai). It solves two fundamental problems of AI finance: *how do you trust that an AI made the decision, and can you trust the quality of the analysis behind it?*

Unlike platforms that rely purely on price signals, **Aletheia's AI agents perform full fundamental analysis** — evaluating on-chain metrics, market sentiment, tokenomic health, and macroeconomic indicators to form a data-grounded investment thesis before any trade is considered. This qualitative and quantitative reasoning layer is what makes Aletheia's decisions defensible, not just reactive.

Every trade Aletheia executes carries a **Proof-of-Inference (PoI)** — a cryptographic signature generated by the 0G Serving network inside a Trusted Execution Environment (TEE). The on-chain `AletheiaVault` smart contract **refuses to execute any trade that lacks a valid PoI**, making the AI's decisions trustless and verifiable by anyone.

> *"Not your keys, not your coins — we applied the same principle to AI decisions."*

---

## 🚦 How to Use Aletheia — Step by Step

Whether you're a crypto veteran or just getting started, this guide walks you through the entire experience from zero to your first AI-executed trade.

---

### Step 1 — Connect Your Wallet

> **Who this is for:** Anyone with an Ethereum-compatible wallet (MetaMask, Rabby, etc.).

1. Open the app at [link](https://aletheia-finance.vercel.app) (or the live deployment URL).
2. Click **"Connect Wallet"** in the top-right corner of the dashboard.
3. Your browser wallet will prompt you to approve the connection — click **Confirm**.
4. Once connected, your wallet address will appear in the header. Aletheia is **non-custodial** — the app never holds your private key.

> 💡 **You are always in control.** The app can only execute trades that you explicitly authorise through a signed strategy. You can withdraw your funds at any time, no questions asked.

---

### Step 2 — Fund Your Vault

Before the AI can trade on your behalf, you need to deposit ETH into your personal `AletheiaVault` — a smart contract deployed on the **0G Galileo Testnet**.

1. From the **Dashboard**, locate the **"My Vault"** panel.
2. Enter the amount of ETH you'd like to deposit (e.g. `0.05 ETH`).
3. Click **"Deposit"** and approve the transaction in your wallet.
4. Your vault balance will update once the transaction is confirmed on-chain.

> ⚠️ **Testnet funds only.** Aletheia currently runs on the 0G Galileo Testnet. You'll need testnet ETH — get some from the [0G Faucet](https://hub.0g.ai/faucet).

---

### Step 3 — Create a Trading Strategy

This is where Aletheia's magic begins. You don't need to write code — just describe your strategy in **plain English**.

1. Navigate to the **"Strategy Lab"** from the sidebar.
2. In the text box, type your strategy as a natural language instruction. Examples:

   ```
   Buy ETH when on-chain active addresses grow by more than 10% in a week
   and exchange outflows are rising, indicating accumulation.
   ```

   ```
   Sell ETH if the price drops more than 15% in 24 hours and social
   sentiment turns negative, suggesting panic selling.
   ```

3. Click **"Compile Strategy"**.

**What happens behind the scenes:**
- The **NLP Compiler** parses your English instruction and converts it into a structured rule set.
- The AI then layers in **fundamental analysis** — evaluating on-chain metrics, tokenomics, macro context, and market sentiment to enrich your rulebook.
- The completed rulebook is **uploaded to 0G decentralised storage**, and you receive a unique **root hash** — a permanent, tamper-proof fingerprint of your strategy.

> 📦 Your strategy rulebook is stored on the decentralised 0G Network. Nobody — not even the Aletheia team — can modify it after it's saved.

---

### Step 4 — Simulate & Activate the AI Agent

With your strategy compiled and stored, you can now activate the AI trading agent.

1. Go back to the **Dashboard** and find your strategy in the **"My Strategies"** panel.
2. Click **"Simulate"** next to the strategy you want to run.

**What happens:**
| Step | What the System Does |
| :--- | :--- |
| 📡 **Fetch Market Data** | The Watcher pulls the live ETH price from CoinGecko and evaluates current market conditions |
| 🔍 **Run Fundamental Analysis** | The AI assesses on-chain metrics, macro signals, and sentiment against your rulebook |
| 🧠 **AI Inference (TEE)** | The decision is computed inside a **Trusted Execution Environment** on the 0G Serving network — meaning even the server operator can't tamper with the result |
| 🔐 **Generate Proof-of-Inference** | The 0G Serving node cryptographically signs the AI's decision, producing a **PoI signature** |
| ⛓️ **On-Chain Settlement** | The backend calls `executeTrade()` on `AletheiaVault.sol`, submitting the decision + PoI signature to the blockchain |
| ✅ **Trade Confirmed** | The smart contract verifies the signature and, if valid, executes the trade — emitting a `TradeExecuted` event |

---

### Step 5 — Read the Trade Proof

After a trade executes, Aletheia shows you the full cryptographic receipt — so you can verify the AI's work yourself.

1. A **"Trade Proof"** modal will appear on the Dashboard automatically after execution.
2. The modal displays:
   - **Trade decision** — Buy / Sell / Hold, and the reasoning
   - **PoI Signature** — the raw cryptographic signature from the 0G Serving node
   - **Transaction hash** — click to view the `TradeExecuted` event live on the [0G Galileo block explorer](https://chainscan-galileo.0g.ai)
   - **Rulebook hash** — verify the strategy that was used on 0G Storage

> 🔎 **Anyone can verify this.** Paste the transaction hash into the block explorer and you'll see the exact parameters of the trade, the PoI payload, and the on-chain confirmation — no trust required.

---

### Step 6 — Withdraw Your Funds

You are always free to withdraw your funds from the vault, with no lock-up periods or fees.

1. On the **Dashboard**, open the **"My Vault"** panel.
2. Enter the amount you'd like to withdraw.
3. Click **"Withdraw"** and approve the transaction in your wallet.
4. Your ETH is returned directly to your wallet — the smart contract sends it with no intermediary.

---

### Quick Reference — Key Concepts

| Term | What It Means |
| :--- | :--- |
| **AletheiaVault** | Your personal smart contract that holds your funds. Only you (and AI-verified trades) can move money out of it. |
| **Proof-of-Inference (PoI)** | A cryptographic signature proving that a 0G AI node (inside a secure TEE) made the trade decision. |
| **Fundamental Analysis** | The AI's deep research layer — evaluating on-chain data, tokenomics, macro trends, and sentiment before deciding. |
| **Strategy Rulebook** | The compiled version of your plain-English strategy, stored permanently on 0G decentralised storage. |
| **TEE (Trusted Execution Env.)** | A secure hardware enclave that runs the AI inference in isolation — no one, not even the node operator, can see or tamper with it. |
| **0G Galileo Testnet** | The test blockchain where Aletheia is currently deployed. Use testnet ETH to try everything for free. |

---

## 🔍 Fundamental Analysis Engine

Most AI trading bots are purely technical — they react to price and volume. Aletheia's agents go deeper.

Before a trade signal is generated, the AI conducts a **multi-layer fundamental analysis** across four domains:

| Layer | What the AI Evaluates |
| :--- | :--- |
| 📊 **On-Chain Metrics** | Active addresses, transaction volume, miner/validator revenue, network growth rate, exchange inflows/outflows |
| 🏛️ **Tokenomics** | Circulating vs. max supply, inflation schedule, token unlock calendars, holder concentration (whale wallets) |
| 🌐 **Macro Context** | BTC dominance, DeFi TVL trends, risk-on/risk-off market regime, stablecoin supply momentum |
| 🗣️ **Sentiment & Narrative** | Developer activity (GitHub commits), social volume, protocol upgrade catalysts, regulatory news |

This analysis is compiled into a **structured rulebook** (stored on 0G decentralized storage) that the agent references at inference time. The AI then reconciles live market price data against this fundamental backdrop to decide whether to act — ensuring trades are grounded in *reason*, not just *reaction*.

> The complete fundamental analysis rulebook for each strategy is hash-anchored on 0G Storage and referenced in the PoI, making the analysis itself tamper-proof and auditable.

---

## ✨ Core Features

| Feature | Description |
| :--- | :--- |
| 🔍 **Fundamental Analysis** | AI agents evaluate on-chain metrics, tokenomics, macro context, and market sentiment before trading |
| 🤖 **NLP Strategy Compiler** | Write trading strategies in plain English — the system compiles them into structured rule logic |
| 🔐 **Proof-of-Inference (PoI)** | Every AI trade decision is signed by a 0G Serving node inside a TEE |
| 🏦 **Trustless Vault** | `AletheiaVault.sol` verifies the PoI signature on-chain before releasing any funds |
| 📡 **Live Market Intelligence** | Real-time ETH price feeds from CoinGecko power the AI decisions |
| 🗄️ **0G Storage** | Strategy rulebooks (including FA logic) are uploaded to decentralized 0G Storage and referenced by root hash |
| 💎 **Glassmorphic UI** | Premium Next.js dashboard with real-time trade proof visualization |

---

## 🏗️ Architecture

```
┌──────────────────────────────────────────────────────────────────┐
│                        Aletheia Finance                          │
├─────────────────┬──────────────────────┬────────────────────────┤
│   Frontend      │      Backend (Go)     │   0G Network           │
│   Next.js 15    │      Gin REST API     │                        │
│                 │                       │   ┌──────────────────┐ │
│  Strategy Lab ──┼──► /strategies ───────┼──►│  0G Storage     │ │
│  (FA Rulebook)  │      ▼ Compile NLP    │   │  (FA Rulebooks)  │ │
│                 │      ▼ FA Analysis    │   └──────────────────┘ │
│                 │      ▼ Upload to 0G   │                        │
│                 │                       │                        │
│  Dashboard    ──┼──► /watcher/execute ──┼──►┌──────────────────┐ │
│   [Simulate]   │      ▼ Live ETH price  │   │  0G Serving      │ │
│       ▼         │      ▼ FA Context     │   │  (TEE Inference) │ │
│  Proof Modal    │      ▼ Request PoI ───┼──►│  + FA Engine     │ │
│  (PoI + Sig)    │      ▼ Verify         │   └──────────────────┘ │
│                 │      ▼ Settle         │                        │
│                 │                       │   ┌──────────────────┐ │
│                 │   VaultService ────────┼──►│  AletheiaVault  │ │
│                 │   (Go Ethereum)        │   │  .sol (Galileo)  │ │
│                 │                       │   └──────────────────┘ │
└─────────────────┴──────────────────────┴────────────────────────┘
```

### The Verifiable Trade Loop

```
User defines a strategy in plain English (NLP Strategy Lab)
    ↓
[CompilerService]  Parses intent → builds structured fundamental analysis rulebook
    ↓
[0G Storage]       Rulebook is uploaded; root hash anchors the FA logic on-chain
    ↓
[WatcherService]   Fetches live ETH price from CoinGecko + evaluates macro signals
    ↓
[ServingService]   Sends market context + FA rulebook hash to 0G Serving node (TEE)
    ↓
[0G Network]       AI runs fundamental analysis + generates trade decision inside TEE, returns PoI signature
    ↓
[VaultService]     Constructs executeTrade() calldata with PoI as payload
    ↓
[AletheiaVault]    Recovers signer from signature → validates against authorizedNodes
    ↓
[On-Chain]         TradeExecuted event emitted on 0G Galileo Testnet ✅
```

---

## 📁 Project Structure

```
aletheia_finance/
├── frontend/               # Next.js 15 application
│   ├── src/app/
│   │   ├── (workspace)/
│   │   │   ├── dashboard/  # Live trading dashboard
│   │   │   └── strategy-lab/ # NLP strategy + FA rulebook creation
│   │   └── page.tsx        # Landing page
│   └── src/lib/api.ts      # Backend API client
│
├── backend/                # Go REST API
│   ├── cmd/api/main.go     # Server entrypoint
│   └── internal/
│       ├── api/
│       │   └── handlers.go # HTTP route handlers
│       ├── services/
│       │   ├── compiler_service.go   # NLP → Fundamental Analysis rulebook compiler
│       │   ├── serving_service.go    # 0G Serving integration (FA + TEE inference)
│       │   ├── vault_service.go      # On-chain settlement (go-ethereum)
│       │   ├── watcher_service.go    # Live market data + macro signal watcher
│       │   └── supabase_service.go   # Database persistence
│       └── og/
│           └── client.go   # 0G Network client (Storage + EVM)
│
├── blockchain/             # Hardhat environment
│   ├── contracts/
│   │   └── AletheiaVault.sol  # Trustless vault smart contract
│   ├── scripts/
│   │   ├── deploy.js          # Deployment script
│   │   └── authorize-node.js  # Node authorization script
│   ├── test/
│   │   └── AletheiaVault.test.js  # 15 unit tests ✅
│   └── hardhat.config.js
│
└── contracts/              # Original contract source
    └── AletheiaVault.sol
```

---

## 🚀 Getting Started

### Prerequisites

- [Go 1.21+](https://go.dev/dl/)
- [Node.js 20+](https://nodejs.org/)
- [Git](https://git-scm.com/)

### 1. Clone the Repository

```bash
git clone https://github.com/OdogwuML/aletheia_finance.git
cd aletheia_finance
```

### 2. Configure Environment Variables

```bash
cp .env.example .env
```

Edit `.env` with your values:

```env
# 0G Network
OG_RPC_URL=https://evmrpc-testnet.0g.ai
OG_STORAGE_ENDPOINT=https://indexer-storage-testnet-turbo.0g.ai
AGENT_PRIVATE_KEY=your_private_key_here

# Deployed Vault Contract
VAULT_ADDRESS=0x9b6eF2eb94Fc756D2521DDc42Baeb4Ec3a3454C4

# Supabase
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_anon_key
```

> ⚠️ **Never commit your `.env` file.** It's already in `.gitignore`.

### 3. Start the Backend

```bash
cd backend/cmd/api
go run main.go
```

The API will be available at `http://localhost:8080`. Verify with:
```bash
curl http://localhost:8080/api/v1/health
```

### 4. Start the Frontend

```bash
cd frontend
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) to access the app.

---

## 🧪 Smart Contract Testing

The `AletheiaVault.sol` contract has a comprehensive test suite covering all security-critical paths.

```bash
cd blockchain
npm install
npx hardhat test
```

**Test coverage:**

```
  AletheiaVault
    📦 Deployment
      ✔ Should set the correct owner
      ✔ Should start with no authorized nodes
    🔐 Node Authorization
      ✔ Owner can authorize a 0G Serving node
      ✔ Owner can deauthorize a node
      ✔ Non-owner CANNOT authorize a node
      ✔ Non-owner CANNOT deauthorize a node
    💰 Deposits & Withdrawals
      ✔ User can deposit ETH into the vault
      ✔ User can withdraw ETH from the vault
      ✔ Withdraw reverts if balance is insufficient
    ⚡ Trade Execution (Proof-of-Inference)
      ✔ PASS: Valid PoI signature from authorized node executes the trade
      ✔ REJECT: Unauthorized signature is rejected
      ✔ REJECT: Expired deadline is rejected
      ✔ REJECT: Insufficient vault balance reverts
      ✔ REJECT: Tampered parameters are rejected
      ✔ REJECT: Deauthorized node after signing is rejected

  15 passing (3s)
```

### Deploy to Galileo Testnet

```bash
# Deploy the vault
npx hardhat run scripts/deploy.js --network galileo

# Authorize a 0G Serving node
npx hardhat run scripts/authorize-node.js --network galileo
```

---

## 🔌 API Reference

| Method | Endpoint | Description |
| :--- | :--- | :--- |
| `GET` | `/api/v1/health` | Health check |
| `POST` | `/api/v1/strategies` | Create and deploy a new AI strategy (with FA rulebook) |
| `GET` | `/api/v1/user/strategies` | Fetch strategies for a wallet address |
| `GET` | `/api/v1/user/stats` | Get credit balance and strategy count |
| `GET` | `/api/v1/watcher/alerts` | Get live market alerts + ETH price + macro signals |
| `POST` | `/api/v1/watcher/execute` | Execute the verifiable AI trade loop (FA → PoI → Settlement) |

**Example — Create Strategy:**
```bash
curl -X POST http://localhost:8080/api/v1/strategies \
  -H "Content-Type: application/json" \
  -d '{
    "logic_nlp": "Buy ETH when on-chain activity surges, exchange outflows rise, and price drops below $2000",
    "owner_address": "0xYourWalletAddress"
  }'
```

**Response:**
```json
{
  "strategy_id": "abc123",
  "message": "Strategy secured on 0G Network",
  "0g_tx_hash": "0x...",
  "credits_paid": 50,
  "rulebook_url": "https://storagescan-galileo.0g.ai/tx/0x..."
}
```

---

## 🔐 Security Model

Aletheia is built on the principle that **neither the developer nor any centralized party can ever move user funds** without a valid AI Proof-of-Inference — one that encodes a verifiable fundamental analysis decision.

```
┌─────────────────────────────────────────────────────────┐
│  Who can trigger a withdrawal?                          │
│                                                         │
│  ✅ User (self) - always, anytime, full control         │
│  ✅ AletheiaVault - ONLY with valid PoI from 0G Serving │
│                                                         │
│  ❌ Developer              - NO access                  │
│  ❌ Random EOA             - NO access                  │
│  ❌ Unverified AI output   - NO access                  │
│  ❌ AI without FA backing  - NO access                  │
└─────────────────────────────────────────────────────────┘
```

The `executeTrade()` function on-chain:
1. Validates the trade hasn't expired
2. Reconstructs the exact message hash from trade parameters
3. Recovers the signer from the PoI signature
4. Checks the signer is in `authorizedNodes` (only set by owner)
5. **If any check fails — reverts. No exceptions.**

---

## 🌐 Deployed Contracts

| Contract | Network | Address |
| :--- | :--- | :--- |
| `AletheiaVault` | 0G Galileo Testnet | [`0x9b6eF2eb94Fc756D2521DDc42Baeb4Ec3a3454C4`](https://chainscan-galileo.0g.ai/address/0x9b6eF2eb94Fc756D2521DDc42Baeb4Ec3a3454C4) |

---

## 🛣️ Roadmap

- [x] NLP Strategy Compiler
- [x] Fundamental Analysis Engine (On-Chain, Tokenomics, Macro, Sentiment)
- [x] 0G Storage Integration (FA Rulebook Upload)
- [x] 0G Serving Integration (Verifiable Inference + FA)
- [x] `AletheiaVault.sol` with PoI-gated settlement
- [x] Live Market Data (CoinGecko)
- [x] Go Ethereum Client (On-Chain Settlement)
- [x] Galileo Testnet Deployment
- [ ] MetaMask Wallet Connection
- [ ] Expanded FA Data Sources (Glassnode, DeFiLlama, Nansen)
- [ ] Multi-asset Strategy Support
- [ ] Strategy Performance Analytics Dashboard
- [ ] Mainnet Deployment

---

## 🤝 Contributing

Contributions are welcome. Please open an issue first to discuss what you would like to change.

1. Fork the repository
2. Create your feature branch (`git checkout -b feature/amazing-feature`)
3. Commit your changes (`git commit -m 'feat: add amazing feature'`)
4. Push to the branch (`git push origin feature/amazing-feature`)
5. Open a Pull Request

---

## 📄 License

This project is licensed under the MIT License — see the [LICENSE](LICENSE) file for details.

---

<div align="center">

Built with ❤️ on the [0G Network](https://0g.ai)

*"Truth in every trade."*

</div>
